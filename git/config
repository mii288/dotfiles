[core]
    editor = vim
    autocrlf = false

[color]
    ui = true

[alias]
    br = branch
    cancel = reset --soft HEAD^
    cm = commit -m
    ap = add -p
    co = checkout
    codev = checkout develop
    coma = checkout master

[push]
    default = current
