[core]
    editor = vim
    autocrlf = false

[color]
    ui = true

[alias]
    ap = add -p
    br = branch
    cancel = reset --soft HEAD^
    cm = commit -m
    co = checkout
    codev = checkout develop
    coma = checkout master

[push]
    default = current
